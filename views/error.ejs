<!DOCTYPE html>
<html lang="ru" data-theme="<%= typeof theme !== 'undefined' ? theme : 'light' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Ошибка' %> | DropAnalyzer</title>
    
    <!-- Стили -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/themes/<%= typeof theme !== 'undefined' ? theme : 'light' %>.css">
    
    <!-- Шрифты -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Основной JavaScript -->
    <script src="/js/main.js" defer></script>
    <script src="/js/theme-switcher.js" defer></script>
</head>
<body data-theme="<%= typeof theme !== 'undefined' ? theme : 'light' %>" class="theme-dependent">
    <header class="main-header theme-dependent">
        <div class="container">
            <a href="/" class="logo">DropAnalyzer</a>
            <nav class="main-nav">
                <ul>
                    <li><a href="/">Главная</a></li>
                    <li><a href="/dashboard">Дашборд</a></li>
                    <li><a href="/analysis">Анализ</a></li>
                    <li><a href="/reports">Отчеты</a></li>
                    <li><a href="/ai-agents">AI-агенты</a></li>
                </ul>
            </nav>
            <button id="theme-toggle" class="theme-toggle" aria-label="Переключить тему">
                <span class="theme-toggle-icon"><%= typeof theme !== 'undefined' && theme === 'light' ? '🌙' : '☀️' %></span>
            </button>
        </div>
    </header>
    
    <main class="main-content theme-dependent">
        <div class="container">
            <div class="error-container">
                <div class="card">
                    <div class="card-header">
                        <h1 class="card-title"><%= typeof title !== 'undefined' ? title : 'Ошибка' %></h1>
                    </div>
                    <div class="card-body">
                        <div class="error-message">
                            <p><%= typeof message !== 'undefined' ? message : 'Произошла ошибка при обработке запроса.' %></p>
                            
                            <% if (typeof error !== 'undefined' && process.env.NODE_ENV !== 'production') { %>
                                <div class="error-details">
                                    <h3>Детали ошибки:</h3>
                                    <pre><%= JSON.stringify(error, null, 2) %></pre>
                                </div>
                            <% } %>
                        </div>
                        
                        <div class="error-actions">
                            <a href="/" class="btn btn-primary">Вернуться на главную</a>
                            <button onclick="window.history.back()" class="btn btn-secondary">Назад</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    
    <footer class="footer theme-dependent">
        <div class="container">
            <div class="footer-content">
                <div class="footer-copyright">
                    &copy; <%= new Date().getFullYear() %> DropAnalyzer. Все права защищены.
                </div>
                <div class="footer-links">
                    <a href="/about">О проекте</a>
                    <a href="/privacy">Конфиденциальность</a>
                    <a href="/terms">Условия использования</a>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
